export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  FRIEND_REQUESTS = 'friend-requests',
  PROFILES = 'profiles',
}

export enum SwaggerApiTags {
  ROOT = 'root',
  AUTH = 'auth',
  USERS = 'users',
  FRIEND_REQUESTS = 'friend-requests',
  PROFILES = 'profiles',
}

export enum Tables {
  USERS = 'users',
  EMAILS = 'emails',
  CONTACTS = 'contacts',
  PROFILES = 'profiles',
  FRIEND_REQUESTS = 'friend-requests',
}
